import wixData from 'wix-data';

export async function get_randomWinner() {
  const { items } = await wixData.query("LotterySubscribers")
    .eq("eventType", "subscribed")
    .find();

  const winner = items[Math.floor(Math.random() * items.length)];
  return { winner };
}
